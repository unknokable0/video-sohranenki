name: Legacy SOHR OAuth Bridge (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SOHR now uses Twitch Device Code Flow; GitHub Pages callback is no longer needed."
